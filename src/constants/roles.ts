/**
 * Các vai trò trong hệ thống LiveKit
 */
export enum UserRole {
    Publisher = 'publisher',    // Người phát livestream (seller)
    Subscriber = 'subscriber',  // Người xem livestream (viewer)
}
